package references;

import java.util.Scanner;

public class InputFromUser {
	public static void main(String[] args) {
		// TODO Auto-generated method stub
		//System.out.println("Price= " + 3*3.99 + "€");
		
		/*
		int count=0;
		for (count=0; count<20; count++) {
			System.out.println(count);
		};
		System.out.println(count);
		*/
		
		Scanner keyboard = new Scanner(System.in);
		int age;
		
		System.out.println("What is your age?");
		age = keyboard.nextInt(); //nextLine if we need a string
		System.out.println("Here is the value that you entered:");
		System.out.println(age);
		
		//Multiply exponent = new Multiply(); //create an item in the this class
		//final double INTEREST_RATE = 0.049; //final means it wont change. Good practice says constants should be in UPPERCASE
		
		keyboard.close(); //close the class, don't know the reason but eclipse does not like it when I leave classes open
	}
}
